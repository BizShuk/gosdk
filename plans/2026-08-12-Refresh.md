# 計畫摘要 (Plans Refresh) — 2026-08-12

本檔整併 2026-07-29 之前的 `plans/` 與 `docs/superpowers/plans/` 開發計畫。
原始文件已刪除；此表是它們留下的唯一紀錄。
`plans/2026-07-29-file-store-unification.md` 在兩週門檻內，`不`納入本次整併。
已淘汰的 API 見 [README.md](../README.md) 的 `歷史側記 (Deprecation Notes)`。

| 日期 | 功能 | 使用方式 | 價值 |
| --- | --- | --- | --- |
| 2026-05-23 | `config` 雙檔載入實作（dual-file loading）：`EnvConfig` / `YamlConfig` / `JsonConfig` 各自載入 base 檔並合併 `.local` 覆寫檔，載入順序 Env → YAML → JSON → `APP_*` | `go run ./config/sample`；覆寫驗證 `APP_SERVER_PORT=9999 go run ./config/sample`；測試 `go test -v ./config` | 開發者以 `.local` 檔覆寫個人設定而不動版控的 base 檔，同時移除 profile 分支帶來的心智負擔 |
| 2026-05-23 | `versioning` semver CLI 實作：`major` / `minor` / `patch` 子命令與 `Version` 結構、`ParseVersion` / `ReadVersion` / `WriteVersion` | `cmd.MajorCmd` / `cmd.MinorCmd` / `cmd.PatchCmd`（原 `./bin/versioning patch` 已淘汰） | 版本號改為單一指令遞增（含 major 重置 minor/patch），避免手改版本檔出錯 |
| 2026-07-01 | `gosdk` 全域架構解耦四階段：`config` 去除對 `utils` 依賴、CSV 移入 `encode/csv`、`db` 顯式建構子、`log.Init()` 導出、`HTTPServer()` 移至 `server` 套件、`metric.OtelMetrics` 對齊 `SPEC.md` | `go test -v ./...`、`go list -f '{{.Imports}}' ./utils` | 消除底層套件的傳遞依賴與 viper 單例強耦合，讓 SDK 可被外部專案當程式庫嵌入並在沙盒環境通過測試 |
| 2026-07-01 | 架構階段四落地：`server.Run(ctx)`（graceful shutdown）與 `metric.OtelMetrics`（`NewOtelMetrics`、`ProcessCounter` / `ProcessHistogram`、`RecordProcessWithDuration`、`Shutdown`）＋ `metric.BatchSummary` / `FormatBatchSummary` | `go run .` 後 `curl -s http://localhost:8080/ping`；測試 `go test -race -v ./metric/... ./server/...` | HTTP 伺服器可被外部專案重用，Go 與 Python 端的 OTel 指標語義與 Tag Keys 完全對齊 |
| 2026-07-05 | 升級專案至 Go 1.26.0：更新 `go.mod`、`build/dockerfile`、`.github/workflows/ci.yml`，並同步 `CLAUDE.md` / `AGENTS.md` / skill 文件的版本標記 | `go mod tidy`，接著 `make test` 與 `make build` | 讓本地、CI 與 Docker 建置環境的 Go 版本一致，避免版本漂移造成建置差異 |
| 2026-07-05 | `log.LevelSplitHandler` 實作任務清單（stdout/stderr 分流 `slog.Handler`）與 `TestLevelSplitHandler` 測試 | `go test ./log`，再以 `make test && make build` 驗證 | 以任務清單追蹤日誌分流功能的落地與驗證，確保每步都有測試背書 |
| 2026-07-05 | `log/levelSplitHandler.go` 拆檔報告：`Enabled` / `Handle` / `WithAttrs` / `WithGroup` 實作，Warn 以上走 stderr、其餘走 stdout | `go test -v ./log` | 日誌依等級分流且不再強行輸出被過濾的訊息，符合 12-factor 的 stdout/stderr 慣例 |
| 2026-07-06 | `config` 套件動態設定重載（dynamic reload）：`fsnotify` 監聽 + debounce + callback 管理，提供 `WithWatch`、`RegisterReloadCallback`、`UnregisterReloadCallback`、`StartWatch`、`StopWatch` | `config.Default(config.WithWatch(nil))`，再以 `config.RegisterReloadCallback(name, cb)` 註冊回呼 | 不重啟服務即可套用新設定（如 `LOG_LEVEL`），維運可熱調參數 |
| 2026-07-06 | 將 `service/generator.go` 遷移為獨立子套件 `service/stringer/generator.go`（`package stringer`，含 `ParsePackage` / `Generate` / `Format`） | `go test -race ./service/...`、`go vet ./...` | 把程式碼產生器與未來業務 `service` 隔離，`service/` 根目錄職責清晰 |
| 2026-07-21 | `cmd.ConfigCmd` 陣列元素級操作（array element-level）：`--append` / `--remove-from`、`ChangeAppended` / `ChangeRemoved` 與 `appendArrayElement` / `removeArrayElement` | `app config --append tags=b`、`app config --remove-from tags=a`；測試 `go test ./cmd/...` | 讓陣列設定不必整段以 JSON literal 覆寫，單一元素的增刪可原子寫入 `settings.local.json` |

## 來源文件 (Absorbed Sources)

| 原始路徑 | 文件日期來源 |
| --- | --- |
| `plans/2026-07-21-config-cmd-array-support.md` | 檔名 |
| `plans/architecture-config-dynamic-reload.md` | `git log` 首次提交 (2026-07-06) |
| `plans/architecture-gosdk.md` | `git log` 首次提交 (2026-07-01) |
| `plans/architecture-service-generator-migration.md` | `git log` 首次提交 (2026-07-06) |
| `plans/implementation_plan.md` | 檔案 mtime (2026-07-05，gitignore 未追蹤) |
| `plans/majestic-swimming-scott.md` | `git log` 首次提交 (2026-07-01) |
| `plans/task.md` | 檔案 mtime (2026-07-05，gitignore 未追蹤) |
| `plans/walkthrough.md` | 檔案 mtime (2026-07-05，gitignore 未追蹤) |
| `docs/superpowers/plans/2026-05-23-config-dual-file.md` | 檔名 |
| `docs/superpowers/plans/2026-05-23-versioning-cli-implementation.md` | 檔名 |
