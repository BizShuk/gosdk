# 規格摘要 (Specs Summary) — 2026-08-12

本檔整併 2026-07-29 之前的 `docs/specs/` 與 `docs/superpowers/specs/` 設計文件。
原始文件已刪除；此表是它們留下的唯一紀錄。
已淘汰的 API 見 [README.md](../../README.md) 的 `歷史側記 (Deprecation Notes)`。

| 日期 | 功能 | 使用方式 | 價值 |
| --- | --- | --- | --- |
| 2026-05-22 | 修復 README 待辦：合併重複 decode/sleep、統一 `gosdk/log`、強型別 `config.ConfigSchema`、`mw.Helmet()` 安全標頭與 CI/Makefile (README problem fixes) | `go vet ./...`、`go test -v ./...`、`make test` | 一次清掉架構重複、日誌不一致、缺測試與缺 CI 的技術債，讓 SDK 有可驗證的基礎 |
| 2026-05-23 | 統一 `config/json.go` 與 `config/embedFS.go` 日誌與 `GetFSReader` 錯誤處理、CSV 邏輯遷入 `encode/csv`（`ProcessCSVFile`、`RecordProcessor`）、`mw.Helmet()` 現代化並補 `utils/` 單元測試 (refactor and test coverage) | `go test -v ./...`、`go vet ./...` | 消除空資料回傳的隱性錯誤，把 CSV 處理歸位到正確套件，同時拉高測試覆蓋率 |
| 2026-05-23 | 將 `ServerConfig` / `DBConnConfig` / `ConfigSchema` 與 `config/db/*` 移轉至 `config/common`，以型別別名維持相容 (config common module extraction) | `make test`、`common.NewDBConfig()` | 收斂設定與 DB 連線的共用型別到單一套件，避免循環相依且不破壞既有呼叫端 |
| 2026-05-23 | `config` 雙檔載入設計（dual-file loading design）：移除 `PROFILE` 與 `GlobalConfig`，改為 base + `.local` 覆寫檔（`.env` / `.env.local`、`config.yaml` / `config.local.yaml`、`settings.json` / `settings.local.json`） | `config.Default()`；覆寫驗證 `APP_SERVER_PORT=9999 go run ./config/sample` | 三種格式載入行為一致、本地覆寫檔可 gitignore，設定改由 viper 單一來源存取 |
| 2026-05-23 | `versioning` CLI 設計：讀寫版本檔並提供 `major` / `minor` / `patch` semver 遞增子命令 (versioning CLI design) | `versioning major` / `versioning minor` / `versioning patch` | 版號遞增變成單一指令，發版流程可重複且不易手誤 |
| 2026-05-27 | `config` 套件的 Functional Option 模式：`ConfigOption`、`WithConfigPath`、`WithDefaultValue`，並讓 `Default()` 接受可變參數 (config options) | `config.Default(config.WithConfigDir("conf"), config.WithDefaultValue("{}"))`；測試 `go test -v ./config/...` | 讓呼叫端可自訂設定路徑並在 `settings.json` 不存在時自動產生預設檔，降低首次啟動摩擦 |
| 2026-06-14 | 帶選項與遞迴備份的寫檔 API：`utils.WriteFile`、`utils.CreateFile`、`WithBackup()`、`WithCreate()`，並重構 `SaveFile` (create file with backup) | `utils.CreateFile(path, payload, utils.WithCreate(), utils.WithBackup())`；測試 `go test -v ./utils` | 覆寫檔案前自動遞迴保留 `.bak`，避免既有設定或資料被無聲覆蓋 |
| 2026-06-15 | 以標準 `log/slog` 取代 `go.uber.org/zap`，`log.Init()` 改讀 viper 的 `LOG_LEVEL` 與 `LOG_FORMAT`（13 個檔案 54 處呼叫） (replace zap with slog) | `log.Init()`；`LOG_LEVEL=debug LOG_FORMAT=json go run .`；驗證 `go vet ./...`、`go test -v ./...` | 移除第三方日誌相依、統一走標準函式庫，同時保留 text/json 與層級切換的部署彈性 |
| 2026-06-22 | `log.LevelSplitHandler`：實作 `slog.Handler` 四方法，Debug/Info 導向 `os.Stdout`、Warn/Error 導向 `os.Stderr`，並修正 `Enabled` 恆為 `true` 的過濾失效 | `go test -v ./log` | 讓日誌可依嚴重度分流到正確 stream，並修好會讓層級過濾完全失效的缺陷 |
| 2026-07-01 | 將 `config/common` 改寫為獨立 `db` 套件：`db.Service` 介面、`db.SQLite` / `db.MySQL` 型別、`db.DefaultSQLite` / `db.DefaultMySQL` 單例與扁平 viper key `SQLITE_PATH` / `MYSQL_DSN` (per-storage singleton) | `db.InitSQLite()` 後以 `db.DefaultSQLite.DB()` 取得 `*gorm.DB`；驗證 `go test ./...` | 每種儲存型態成為型別安全的 service singleton，命名與其他模組一致，並移除 junk drawer 式的 `config/common` |
| 2026-07-01 | 跨語言 Process Metrics 規格：Counter/Histogram/Gauge 命名（`process.ticker.fetch`、`queue.job.execute.duration`）與標準 Tag keys（`domain`、`service`、`status`、`error_type` 等），搭配 `metric/` OTel 封裝與 `notify/slack.go` | `go test -v ./notify/...`、`go test -v ./metric/...` | 讓 Go 與 Python 服務用同一套維度追蹤批次、佇列與 API，失敗可即時與批次總結通知 |

## 來源文件 (Absorbed Sources)

| 原始路徑 | 文件日期來源 |
| --- | --- |
| `docs/specs/2026-05-22-fix-readme-problems.md` | 檔名 |
| `docs/specs/2026-05-23-refactor-and-test-coverage.md` | 檔名 |
| `docs/specs/2026-05-23-refactor-config-common.md` | 檔名 |
| `docs/specs/2026-05-27-config-options.md` | 檔名 |
| `docs/specs/2026-06-14-create-file-with-backup.md` | 檔名 |
| `docs/specs/2026-06-15-replace-zap-with-slog.md` | 檔名 |
| `docs/specs/2026-06-22-level-split-handler.md` | 檔名 |
| `docs/specs/cryptic-pondering-pie.md` | `git log` 首次提交 (2026-07-01) |
| `docs/specs/prancy-imagining-meteor.md` | `git log` 首次提交 (2026-07-01) |
| `docs/superpowers/specs/2026-05-23-config-dual-file-design.md` | 檔名 |
| `docs/superpowers/specs/2026-05-23-versioning-cli-design.md` | 檔名 |
